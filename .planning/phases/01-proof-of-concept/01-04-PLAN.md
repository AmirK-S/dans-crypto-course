---
phase: 01-proof-of-concept
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - output/loom-test-results.json
autonomous: true
gap_closure: true
requirements:
  - EXTR-01
  - EXTR-02
  - EXTR-03

must_haves:
  truths:
    - "Enriched manifest passes structural validation with ~13 sections and ~80-95 videos"
    - "validate-manifest.mjs exits 0 (PASS) or 2 (WARN) — not 1 (FAIL)"
    - "Loom batch test fetches transcripts for at least 5 real video IDs from the enriched manifest"
    - "All 5+ tested transcripts contain non-empty plain text content (words > 0)"
    - "Test results span multiple sections (not all from one section)"
  artifacts:
    - path: "output/loom-test-results.json"
      provides: "Updated Loom batch test results with 5+ successful transcript fetches"
      contains: "videoId"
  key_links:
    - from: "scripts/validate-manifest.mjs"
      to: "output/manifest.json"
      via: "fs.readFileSync + JSON.parse — reads enriched manifest"
      pattern: "manifest.json"
    - from: "scripts/test-loom-batch.mjs"
      to: "output/manifest.json"
      via: "reads enriched manifest to extract 5+ loomIds for testing"
      pattern: "manifest.json"
    - from: "scripts/test-loom-batch.mjs"
      to: "https://www.loom.com/graphql"
      via: "POST FetchVideoTranscript for each of 5+ test IDs"
      pattern: "loom.com/graphql"
    - from: "scripts/test-loom-batch.mjs"
      to: "output/loom-test-results.json"
      via: "fs.writeFileSync with test results JSON"
      pattern: "loom-test-results.json"
---

<objective>
Re-validate the enriched manifest and re-run the Loom batch transcript test against 5+ real video IDs from Dan's course. This closes the remaining Phase 1 verification gaps: the manifest must pass validation with ~13 sections, and the Loom API must be proven against at least 5 real course video IDs.

Purpose: Plan 01-03 enriched the manifest with real section names and loomIds. This plan confirms the enrichment worked (validation passes) and that the Loom transcript pipeline works at scale (5+ real videos, not just 2). Together, 01-03 + 01-04 close all three gaps from 01-VERIFICATION.md.

Output: Updated `output/loom-test-results.json` with 5+ successful transcript fetches
</objective>

<execution_context>
@/Users/amir/.ai/get-shit-done/workflows/execute-plan.md
@/Users/amir/.ai/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-proof-of-concept/01-RESEARCH.md
@.planning/phases/01-proof-of-concept/01-02-SUMMARY.md
@.planning/phases/01-proof-of-concept/01-VERIFICATION.md
@.planning/phases/01-proof-of-concept/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-validate enriched manifest and re-test Loom API for 5+ real video IDs</name>
  <files>output/loom-test-results.json</files>
  <action>
This task re-runs the TWO existing validation scripts from Plan 01-02 against the newly enriched `output/manifest.json` from Plan 01-03. No new scripts are created.

**Step 1: Re-validate the enriched manifest**

Run the existing validation script:
```bash
node scripts/validate-manifest.mjs
```

**Expected outcome:** The script should now exit 0 (PASS) or 2 (WARN) instead of 1 (FAIL), because:
- Section count should be ~13 (was 1 "Unknown Section")
- Video count should still be ~95
- Most videos should have non-null loomIds

**If validation FAILS (exit 1):**
- Check the console output for the specific failure reason
- If section count is still < 5: The enrichment script may not have worked correctly. Check `output/manifest.json` manually. Log the issue but continue to Step 2 (the Loom test is independent of section grouping).
- If video count is < 20: Something went wrong with the enrichment — likely data loss. This is a blocker that requires re-running Plan 01-03.

**Step 2: Re-run Loom batch transcript test**

Run the existing batch test script:
```bash
node scripts/test-loom-batch.mjs
```

**Expected outcome:** The script should now:
- Find 5+ videos with non-null loomIds in the enriched manifest (was only 1 before)
- Select 5 IDs spread across different sections
- Fetch transcripts for all 5 via Loom GraphQL API
- Write updated results to `output/loom-test-results.json`
- Exit 0 (all succeeded) or 2 (partial success)

The test-loom-batch.mjs script from Plan 01-02 already handles all this logic — it reads manifest.json, selects up to 5 IDs across sections, fetches via GraphQL, and writes results. The enriched manifest simply provides the data it needs.

**If the batch test only tests fewer than 5:**
- The enriched manifest may have fewer than 5 non-null loomIds. Check the manifest.
- If the manifest has 5+ loomIds but the script only tests fewer, check the section-spread selection logic in test-loom-batch.mjs.

**If all 5 tests FAIL (exit 1):**
- Log: "All transcript fetches failed. This may indicate an auth requirement for Dan's course videos."
- Check if the error is HTTP 400 (body format issue — should not happen) or null captions_source_url (possible auth/processing issue).
- Compare against the 2 successful tests from Plan 01-02 — those IDs should still work.

**Step 3: Verify results**

After both scripts run, verify:
```bash
node -e "const m=JSON.parse(require('fs').readFileSync('output/manifest.json','utf8')); const r=JSON.parse(require('fs').readFileSync('output/loom-test-results.json','utf8')); console.log('Sections:', m.sections.length, '| Videos:', m.totalVideos, '| WithLoom:', m.sections.flatMap(s=>s.videos).filter(v=>v.loomId).length, '| Tested:', r.totalTested, '| Successful:', r.successful)"
```

This should show: Sections >= 5 | Videos ~95 | WithLoom >> 1 | Tested >= 5 | Successful >= 5

Log the output of this verification command as the definitive proof of gap closure.
  </action>
  <verify>
    <automated>node scripts/validate-manifest.mjs 2>&1; echo "Validate exit: $?"; echo "---"; node scripts/test-loom-batch.mjs 2>&1 | tail -20; echo "Batch exit: $?"; echo "---"; node -e "const r=JSON.parse(require('fs').readFileSync('output/loom-test-results.json','utf8')); console.log('Tested:', r.totalTested, 'Successful:', r.successful); process.exit(r.successful >= 5 ? 0 : 1)"</automated>
  </verify>
  <done>
- validate-manifest.mjs exits 0 (PASS) or 2 (WARN) — not 1 (FAIL)
- Manifest shows ~13 sections with real names (not "Unknown Section")
- test-loom-batch.mjs tests at least 5 real video IDs from the enriched manifest
- At least 5 transcripts fetched successfully with non-empty content
- output/loom-test-results.json updated with 5+ successful test results
- Verification command shows Sections >= 5, Tested >= 5, Successful >= 5
  </done>
</task>

</tasks>

<verification>
After task completes, verify all three Phase 1 success criteria from ROADMAP are now met:

1. **SC1: Browser console script extracts Loom embed URLs with video titles and section names:**
   - Confirmed by: enriched `output/manifest.json` has ~13 sections with real names + loomIds for most videos
   - Check: `node -e "const m=JSON.parse(require('fs').readFileSync('output/manifest.json','utf8')); console.log('Sections:', m.sections.length, 'Videos:', m.totalVideos); m.sections.forEach(s => console.log('  ' + s.order + '. ' + s.name + ' (' + s.videos.length + ' videos, ' + s.videos.filter(v=>v.loomId).length + ' with loomId)'))"`

2. **SC2: Extracted data exports as valid JSON manifest with section hierarchy intact and ~80 videos across ~13 sections:**
   - Confirmed by: `node scripts/validate-manifest.mjs` exits 0 or 2

3. **SC3: Standalone test script fetches plain-text transcript for at least 5 real Loom video IDs:**
   - Confirmed by: `output/loom-test-results.json` shows `successful >= 5`
   - Check: `node -e "const r=JSON.parse(require('fs').readFileSync('output/loom-test-results.json','utf8')); console.log('Success:', r.successful, '/', r.totalTested)"`
</verification>

<success_criteria>
- Manifest validation passes (exit 0 or 2) with ~13 sections and ~80-95 videos
- Loom batch test succeeds for at least 5 video IDs (exit 0)
- output/loom-test-results.json updated with 5+ successful fetches
- All three Phase 1 success criteria from ROADMAP are fully satisfied
- All three gaps from 01-VERIFICATION.md are closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-proof-of-concept/01-04-SUMMARY.md`
</output>
