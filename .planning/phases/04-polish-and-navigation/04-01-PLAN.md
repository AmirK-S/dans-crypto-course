---
phase: 04-polish-and-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-takeaways-and-cheatsheets.mjs
  - output/transcripts/01-introduction-to-the-course/*.md
  - output/transcripts/03-mindset/*.md
  - output/transcripts/04-market-psychology/*.md
  - output/transcripts/05-understanding-crypto-cycles-price-action/*.md
  - output/transcripts/06-do-not-do-list/*.md
  - output/transcripts/07-fundamental-analysis/*.md
  - output/transcripts/08-technical-analysis/*.md
  - output/transcripts/09-building-a-strategy-edge/*.md
  - output/transcripts/10-scams-to-avoid/*.md
  - output/transcripts/11-taxes-and-banking-infrastructure/*.md
  - output/transcripts/13-best-online-income-sources-to-get-for-crypto/*.md
  - output/transcripts/*/CHEAT_SHEET.md
autonomous: true
requirements: [MKDN-05, SUMM-04]
user_setup:
  - service: ai-vendor
    why: "AI API calls for per-video key takeaways and per-section cheat sheets"
    env_vars:
      - name: AI_API_KEY
        source: "console.ai-vendor.com -> API Keys -> Create Key"

must_haves:
  truths:
    - "Each video markdown file with real transcript content contains a ## Key Takeaways section with 3-7 bullet points before the ## Transcript section"
    - "YouTube stub files and short boilerplate files do NOT have a ## Key Takeaways section (no fabrication from titles alone)"
    - "Each section folder with a real SECTION_SUMMARY.md (>500 chars) contains a CHEAT_SHEET.md with ultra-condensed rules, entry signals, and warnings"
    - "Cheat sheets are distinct from section summaries: no prose, no explanations, one-line bullets only, scannable in 30 seconds"
    - "Re-running the script skips already-processed files (idempotent)"
  artifacts:
    - path: "scripts/generate-takeaways-and-cheatsheets.mjs"
      provides: "AI-driven generation script for key takeaways and cheat sheets"
      contains: "ai-model-haiku"
    - path: "output/transcripts/*/CHEAT_SHEET.md"
      provides: "Per-section quick-reference cheat sheets (12 real + 0 stubs)"
  key_links:
    - from: "scripts/generate-takeaways-and-cheatsheets.mjs"
      to: "ai-sdk"
      via: "client.messages.create() with ai-model-haiku"
      pattern: "new AI vendor.*messages\\.create"
    - from: "video .md files"
      to: "## Key Takeaways section"
      via: "in-place insertion before ## Transcript heading"
      pattern: "## Key Takeaways"
---

<objective>
Generate per-video key takeaways (3-7 bullets) and per-section cheat sheets using AI model (Haiku) API calls, producing scannable reference content for the entire knowledge base.

Purpose: Transform the knowledge base from transcript-heavy reference material into an actionable trading system with quick-reference extraction at both video and section levels.
Output: 74 video files modified with `## Key Takeaways` sections + 12 `CHEAT_SHEET.md` files created
</objective>

<execution_context>
@/Users/amir/.ai/get-shit-done/workflows/execute-plan.md
@/Users/amir/.ai/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-and-navigation/04-RESEARCH.md
@.planning/phases/03-ai-summarization/03-02-SUMMARY.md

<interfaces>
<!-- Existing patterns from Phase 3 that this plan follows -->

From scripts/generate-summaries.mjs (Phase 3 reference):
- ESM imports: `import AI vendor from 'ai-sdk'`
- Client init: `new AI vendor({ apiKey: process.env.AI_API_KEY })`
- API call: `client.messages.create({ model, max_tokens, temperature: 0, system, messages })`
- SDK handles 429 retry automatically

From output/transcripts/ file structure:
- 13 section directories (01-introduction-to-the-course through 13-best-online-income-sources-to-get-for-crypto)
- 95 video .md files total: 74 real Loom transcripts, 19 YouTube stubs, 2 text stubs
- 12 SECTION_SUMMARY.md files with real content, 1 stub (section 02)
- Section 12 has only SECTION_SUMMARY.md (0 video files)

Video .md file structure (for in-place modification):
```
# Video Title

**Section:** Section Name
**Loom ID:** abc123 (or **Source:** YouTube/Skool)

## Transcript

[transcript text here]
```

After modification:
```
# Video Title

**Section:** Section Name
**Loom ID:** abc123

## Key Takeaways

- [bullet 1]
- [bullet 2]
- ...

## Transcript

[transcript text here]
```
</interfaces>

**CRITICAL Phase 3 decision:** Phase 3 Plan 02 deviated from API calls to AI assistant direct generation because user had no API key. However, the script `generate-summaries.mjs` was still created as a reusable tool. For Phase 4, create the script AND attempt to run it. If AI_API_KEY is not set, the script should exit with a clear error message. The executor should then fall back to AI assistant direct generation (reading each transcript and writing takeaways/cheat sheets directly), following the Phase 3 established pattern.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generate-takeaways-and-cheatsheets.mjs script</name>
  <files>scripts/generate-takeaways-and-cheatsheets.mjs</files>
  <action>
Create `scripts/generate-takeaways-and-cheatsheets.mjs` — a Node.js ESM script that:

**WAVE 1 — Key Takeaways (MKDN-05):**
1. Scans all 13 section directories in `output/transcripts/`
2. For each video `.md` file (excluding SECTION_SUMMARY.md, CHEAT_SHEET.md, INDEX.md):
   - **Idempotency check:** Skip if file already contains `## Key Takeaways` (log `[SKIP]`)
   - **Content validation — skip if ANY of these are true:**
     - File contains `YouTube video embed` (YouTube stub)
     - Word count < 100 (boilerplate/empty)
     - No `## Transcript` heading exists
     - Transcript content after `## Transcript` heading is < 100 chars
   - Extract video title from first `# ` line
   - Extract transcript text from content after `## Transcript` heading
   - Call AI model (Haiku) API with extractive prompt (see prompt below)
   - Insert `## Key Takeaways` section BEFORE the `## Transcript` heading using `indexOf()` (NOT string.replace)
   - Write modified file back in-place
   - Log `[OK] filename — N bullets`
3. Print summary: `Key Takeaways: X generated, Y skipped`

**WAVE 2 — Cheat Sheets (SUMM-04):**
4. For each section directory:
   - **Idempotency check:** Skip if `CHEAT_SHEET.md` already exists (log `[SKIP]`)
   - Skip if `SECTION_SUMMARY.md` doesn't exist
   - Skip if `SECTION_SUMMARY.md` content length < 500 chars (stub — prevents fabrication)
   - Read `SECTION_SUMMARY.md` content
   - Derive human-readable section name from directory name (strip numeric prefix, replace dashes with spaces, title case)
   - Call AI model (Haiku) API with cheat sheet prompt (see prompt below)
   - Write result to `CHEAT_SHEET.md` in the section directory
   - Log `[OK] section-name`
5. Print summary: `Cheat Sheets: X generated, Y skipped`

**Configuration:**
- Model: `ai-model-haiku`
- Temperature: 0 (extractive, no creativity)
- Max tokens for takeaways: 512
- Max tokens for cheat sheets: 1024
- Inter-call delay: 1000ms
- AI_API_KEY guard at top (exit with clear error if missing)

**Key Takeaway System Prompt:**
```
You are a trading analyst extracting key takeaways from a crypto course video transcript.
Extract only what the instructor EXPLICITLY STATES. Do NOT add external knowledge or trading theory.
Do NOT fabricate numbers, indicators, or rules not mentioned in the transcript.
```

**Key Takeaway User Prompt:**
```
Extract 3-7 key takeaway bullets from this crypto course video: "{title}"

<transcript>
{transcriptText}
</transcript>

Write ONLY a bullet list (3-7 items). Each bullet must:
- Start with "- "
- Be a single concise sentence
- Capture only what the instructor explicitly teaches
- Use the instructor's own phrasing where possible

Do not preamble. Do not number the bullets. Output only the bullet list.
```

**Cheat Sheet System Prompt:**
```
You are condensing a detailed trading playbook summary into an ultra-compact cheat sheet.
The cheat sheet must be scannable in under 30 seconds during live trading.
Extract ONLY explicit rules, entry signals, and warnings. No prose. No explanations. Maximum density.
Only include information explicitly present in the summary. Do not add external knowledge.
```

**Cheat Sheet User Prompt:**
```
Create an ultra-compact cheat sheet from this section summary for "{sectionName}".

<section_summary>
{sectionSummaryText}
</section_summary>

Write the cheat sheet in this exact format:

# {sectionName} — Quick Reference Cheat Sheet

## Rules
- [one-line rule]

## Entry Signals
- [one-line signal]

## Warnings / Do Not Do
- [one-line warning]

Only include entries that are EXPLICITLY STATED in the summary.
Omit any section heading if no explicit content exists for it. Do not preamble.
```

**In-place insertion function (critical — use indexOf, NOT replace):**
```javascript
function insertKeyTakeaways(fileContent, takeawaysText) {
  const marker = '\n## Transcript';
  const idx = fileContent.indexOf(marker);
  if (idx === -1) return fileContent;
  return (
    fileContent.slice(0, idx) +
    '\n\n## Key Takeaways\n\n' + takeawaysText +
    fileContent.slice(idx)
  );
}
```

Follow the exact patterns from `scripts/generate-summaries.mjs` for ESM imports, AI vendor client setup, and error handling. Use `ai-sdk` (already installed at 0.78.0).
  </action>
  <verify>
    <automated>node scripts/generate-takeaways-and-cheatsheets.mjs --help 2>&1 | head -5 || node -e "import('./scripts/generate-takeaways-and-cheatsheets.mjs').catch(e => { if (e.message.includes('AI_API_KEY')) { console.log('PASS: API key guard works'); process.exit(0); } else { console.error(e); process.exit(1); } })"</automated>
  </verify>
  <done>Script file exists at scripts/generate-takeaways-and-cheatsheets.mjs, is valid ESM, imports ai-sdk, has AI_API_KEY guard, implements both key takeaway generation with in-place insertion and cheat sheet generation with idempotency checks</done>
</task>

<task type="auto">
  <name>Task 2: Generate all key takeaways and cheat sheets</name>
  <files>output/transcripts/**/*.md, output/transcripts/*/CHEAT_SHEET.md</files>
  <action>
Run the script to generate all key takeaways and cheat sheets:

```bash
node scripts/generate-takeaways-and-cheatsheets.mjs
```

**If AI_API_KEY is set:** Script runs and generates all output. Verify counts match expectations:
- Key takeaways: ~74 generated (real transcript files), 0 for YouTube stubs
- Cheat sheets: ~12 generated (sections with real SECTION_SUMMARY.md), 0-1 skipped (section 02 stub)

**If AI_API_KEY is NOT set (Phase 3 precedent):** Fall back to AI assistant direct generation, following the pattern established in Phase 3 Plan 02:

For **key takeaways** (MKDN-05):
- Read each video .md file with real transcript content (skip YouTube stubs, skip if word count < 100, skip if no `## Transcript` heading)
- Skip files that already have `## Key Takeaways`
- For each file: extract the transcript text, generate 3-7 key takeaway bullets following the extractive prompt (only what instructor explicitly states, no fabrication), insert as `## Key Takeaways` section before `## Transcript`
- Write modified file back

For **cheat sheets** (SUMM-04):
- Read each SECTION_SUMMARY.md (skip if < 500 chars or already has CHEAT_SHEET.md)
- Generate ultra-condensed cheat sheet: Rules, Entry Signals, Warnings — one-line bullets only, no prose
- Write to CHEAT_SHEET.md in same directory

**Anti-hallucination rules (apply to both API and direct generation):**
- Only include information explicitly stated in the source transcript/summary
- Do NOT fabricate numbers, indicators, thresholds, or rules
- Use the instructor's own phrasing where possible
- Temperature 0 (if API) / extractive-only (if direct)

**Expected output:**
- ~74 video files modified with `## Key Takeaways` section
- ~12 CHEAT_SHEET.md files created
- 0 YouTube stub files modified
- Section 02 and section 12 skipped for cheat sheets (stub/no-content summaries)
  </action>
  <verify>
    <automated>echo "=== Key Takeaways ===" && grep -rl "## Key Takeaways" output/transcripts/ | wc -l && echo "=== Cheat Sheets ===" && find output/transcripts -name "CHEAT_SHEET.md" | wc -l && echo "=== YouTube stubs with takeaways (should be 0) ===" && grep -rl "YouTube video embed" output/transcripts/ | xargs grep -l "## Key Takeaways" 2>/dev/null | wc -l && echo "=== Cheat sheet format check ===" && head -5 output/transcripts/03-mindset/CHEAT_SHEET.md 2>/dev/null</automated>
  </verify>
  <done>At least 70 video files contain ## Key Takeaways section with 3-7 bullets each. At least 10 CHEAT_SHEET.md files exist in section directories. 0 YouTube stub files have been modified with takeaways. Cheat sheets use ultra-condensed format (Rules, Entry Signals, Warnings sections).</done>
</task>

<task type="auto">
  <name>Task 3: Verify quality — spot-check takeaways and cheat sheets against source content</name>
  <files></files>
  <action>
Spot-check 5 key takeaways and 3 cheat sheets for quality and anti-hallucination:

**Key Takeaway Spot-Check (5 files from different sections):**
1. Pick one file from section 01 (introduction)
2. Pick one file from section 07 (fundamental analysis — dense content)
3. Pick one file from section 08 (technical analysis — specific indicators)
4. Pick one file from section 09 (strategy/edge — actionable rules)
5. Pick one file from section 10 (scams — warning content)

For each:
- Read the `## Key Takeaways` section
- Read the `## Transcript` section
- Verify every takeaway bullet can be traced to specific transcript content
- Flag any bullet that contains information NOT in the transcript (hallucination)
- Verify bullet count is 3-7
- Verify takeaways appear BEFORE transcript (correct insertion position)

**Cheat Sheet Spot-Check (3 files):**
1. Section 06 (do-not-do-list — should have clear warnings)
2. Section 08 (technical-analysis — should have entry signals)
3. Section 09 (building-a-strategy-edge — should have rules)

For each:
- Read CHEAT_SHEET.md
- Read SECTION_SUMMARY.md
- Verify every cheat sheet line traces to summary content
- Verify format: no prose paragraphs, one-line bullets only
- Verify distinctness from summary (condensed, not a copy)

**Report format:**
```
SPOT-CHECK RESULTS
==================
Key Takeaways: X/5 passed (0 hallucinations detected)
Cheat Sheets: X/3 passed (format correct, no fabrication)
```

If any hallucination detected: log the specific bullet and source file. This is a verification task — do NOT modify files.
  </action>
  <verify>
    <automated>echo "=== Takeaway position check (should appear before Transcript) ===" && for f in output/transcripts/07-fundamental-analysis/01-*.md output/transcripts/08-technical-analysis/01-*.md; do if [ -f "$f" ]; then KT=$(grep -n "## Key Takeaways" "$f" | head -1 | cut -d: -f1); TR=$(grep -n "## Transcript" "$f" | head -1 | cut -d: -f1); echo "$f: Takeaways@$KT Transcript@$TR $([ "$KT" -lt "$TR" ] && echo 'ORDER_OK' || echo 'ORDER_FAIL')"; fi; done && echo "=== Cheat sheet prose check (lines > 100 chars, should be few) ===" && awk 'length > 100' output/transcripts/06-do-not-do-list/CHEAT_SHEET.md 2>/dev/null | wc -l</automated>
  </verify>
  <done>5 key takeaways spot-checked: all bullets traceable to source transcripts, 0 hallucinations, correct insertion position (before ## Transcript). 3 cheat sheets spot-checked: ultra-condensed format confirmed, no prose paragraphs, content traces to SECTION_SUMMARY.md.</done>
</task>

</tasks>

<verification>
1. `grep -rl "## Key Takeaways" output/transcripts/ | wc -l` returns >= 70 (74 expected)
2. `find output/transcripts -name "CHEAT_SHEET.md" | wc -l` returns >= 10 (12 expected)
3. `grep -rl "YouTube video embed" output/transcripts/ | xargs grep -l "## Key Takeaways" 2>/dev/null | wc -l` returns 0 (no fabrication from stubs)
4. For 5 randomly selected takeaway files: every bullet traces to explicit transcript content
5. Cheat sheets contain no prose paragraphs (ultra-condensed format)
6. Script is idempotent: re-running produces `[SKIP]` for all already-processed files
</verification>

<success_criteria>
- At least 70 video markdown files have `## Key Takeaways` (3-7 bullets) inserted before `## Transcript`
- At least 10 section folders contain `CHEAT_SHEET.md` with ultra-condensed format
- 0 YouTube stub files have been modified with fabricated takeaways
- Spot-check of 5 takeaways confirms 0 hallucinated bullets
- Spot-check of 3 cheat sheets confirms condensed format distinct from SECTION_SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-and-navigation/04-01-SUMMARY.md`
</output>
