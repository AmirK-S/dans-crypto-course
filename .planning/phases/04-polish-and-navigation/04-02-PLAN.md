---
phase: 04-polish-and-navigation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - scripts/generate-indexes.mjs
  - output/transcripts/01-introduction-to-the-course/INDEX.md
  - output/transcripts/02-the-basics/INDEX.md
  - output/transcripts/03-mindset/INDEX.md
  - output/transcripts/04-market-psychology/INDEX.md
  - output/transcripts/05-understanding-crypto-cycles-price-action/INDEX.md
  - output/transcripts/06-do-not-do-list/INDEX.md
  - output/transcripts/07-fundamental-analysis/INDEX.md
  - output/transcripts/08-technical-analysis/INDEX.md
  - output/transcripts/09-building-a-strategy-edge/INDEX.md
  - output/transcripts/10-scams-to-avoid/INDEX.md
  - output/transcripts/11-taxes-and-banking-infrastructure/INDEX.md
  - output/transcripts/12-putting-everything-together/INDEX.md
  - output/transcripts/13-best-online-income-sources-to-get-for-crypto/INDEX.md
  - output/COURSE_INDEX.md
autonomous: true
requirements: [SUMM-05]

must_haves:
  truths:
    - "Each of the 13 section folders contains an INDEX.md linking to all video files, SECTION_SUMMARY.md, and CHEAT_SHEET.md (when present) in that section"
    - "A master COURSE_INDEX.md at output/ root links to all 13 section indexes, all video files, and all summary/cheat sheet files"
    - "All markdown links use relative paths (no absolute paths, no /Users/ paths)"
    - "Section 12 INDEX.md exists and gracefully handles having 0 video files"
    - "YouTube stub files in section 02 appear in the index (they exist in the knowledge base)"
    - "Re-running the script overwrites index files with current state (always idempotent)"
  artifacts:
    - path: "scripts/generate-indexes.mjs"
      provides: "Pure filesystem traversal script for index generation — no API calls"
    - path: "output/COURSE_INDEX.md"
      provides: "Master course index linking all 13 sections"
      contains: "Bull Run Millions"
    - path: "output/transcripts/*/INDEX.md"
      provides: "Per-section index files (13 total)"
  key_links:
    - from: "output/COURSE_INDEX.md"
      to: "output/transcripts/*/INDEX.md"
      via: "relative markdown links"
      pattern: "transcripts/.*/INDEX\\.md"
    - from: "output/transcripts/*/INDEX.md"
      to: "video .md files, SECTION_SUMMARY.md, CHEAT_SHEET.md"
      via: "same-directory relative links"
      pattern: "\\]\\([^/].*\\.md\\)"
---

<objective>
Generate section-level and course-level index files that link the entire knowledge base together into a navigable reference system.

Purpose: Make the knowledge base usable as a day-to-day trading reference by providing structured navigation from course overview down to individual videos, with links to summaries and cheat sheets at every level.
Output: 13 per-section `INDEX.md` files + 1 master `output/COURSE_INDEX.md`
</objective>

<execution_context>
@/Users/amir/.ai/get-shit-done/workflows/execute-plan.md
@/Users/amir/.ai/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-and-navigation/04-RESEARCH.md
@.planning/phases/04-polish-and-navigation/04-01-SUMMARY.md

<interfaces>
<!-- Filesystem structure from output/transcripts/ -->

13 section directories:
- 01-introduction-to-the-course (5 video files + SECTION_SUMMARY.md + CHEAT_SHEET.md)
- 02-the-basics (4 video files + SECTION_SUMMARY.md, no CHEAT_SHEET.md — stub summary)
- 03-mindset through 11-taxes-and-banking-infrastructure (variable video files + SECTION_SUMMARY.md + CHEAT_SHEET.md)
- 12-putting-everything-together (0 video files, SECTION_SUMMARY.md only — no CHEAT_SHEET.md)
- 13-best-online-income-sources-to-get-for-crypto (video files + SECTION_SUMMARY.md + CHEAT_SHEET.md)

Video file naming pattern: NN-slug-title.md (e.g., 01-introduction.md)

Additional file at output/ root:
- output/MASTER_SUMMARY.md (course-level trading playbook from Phase 3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generate-indexes.mjs script and run it</name>
  <files>scripts/generate-indexes.mjs, output/transcripts/*/INDEX.md, output/COURSE_INDEX.md</files>
  <action>
Create `scripts/generate-indexes.mjs` — a pure Node.js ESM script (NO API calls, NO external dependencies) that:

1. **Scans** `output/transcripts/` for all section directories (sorted alphabetically)
2. **For each section directory**, generates `INDEX.md` with:
   - Title: `# {Human-Readable Section Name} — Index` (derive from directory name: strip numeric prefix, replace dashes with spaces, title case)
   - **Reference Files** subsection (if present):
     - Link to `SECTION_SUMMARY.md` if it exists
     - Link to `CHEAT_SHEET.md` if it exists
   - **Videos** subsection:
     - One link per video `.md` file (exclude SECTION_SUMMARY.md, CHEAT_SHEET.md, INDEX.md)
     - Derive readable title from filename: strip numeric prefix, remove `.md`, replace dashes with spaces, title case
     - If 0 video files: show `*No video files in this section.*` instead
   - ALL links must be relative (same-directory: just the filename, e.g., `[Summary](SECTION_SUMMARY.md)`)
   - Write to `{sectionDir}/INDEX.md`

3. **Generates master `output/COURSE_INDEX.md`** with:
   - Title: `# Bull Run Millions Crypto Course — Master Index`
   - Stats line: `**{N} sections** · **{M} videos** · Generated: {YYYY-MM-DD}`
   - Link to `[Master Summary](MASTER_SUMMARY.md)` (relative from output/)
   - Horizontal rule separator
   - For each section:
     - Section heading as link to section INDEX.md: `## [{Section Name}](transcripts/{dirName}/INDEX.md)`
     - Metadata line: video count + link to Summary + link to Cheat Sheet (if they exist)
     - List of all video files as links: `- [{Title}](transcripts/{dirName}/{filename})`
   - ALL links must be relative from `output/` directory

**Helper functions:**
```javascript
function humanizeFilename(filename) {
  return filename
    .replace(/^\d+-/, '')       // Remove numeric prefix
    .replace(/\.md$/, '')        // Remove extension
    .replace(/-/g, ' ')          // Dashes to spaces
    .replace(/\b\w/g, c => c.toUpperCase());  // Title case
}

function humanizeSectionName(dirName) {
  return dirName
    .replace(/^\d+-/, '')
    .replace(/-/g, ' ')
    .replace(/\b\w/g, c => c.toUpperCase());
}
```

**Section 12 handling:** Section 12 (putting-everything-together) has 0 video files. The INDEX.md should still be generated with a `*No video files in this section.*` message. The COURSE_INDEX.md should still include section 12 (not skip it).

**Section 02 handling:** Section 02 has 4 YouTube stub files. They should appear in the index as normal video links — they ARE part of the knowledge base even though they lack Loom transcripts.

**Idempotency:** Script always overwrites all INDEX.md and COURSE_INDEX.md files — this is correct because indexes should always reflect current filesystem state. No skip-if-exists check needed.

Run the script after creation:
```bash
node scripts/generate-indexes.mjs
```

Verify all 13 INDEX.md files + 1 COURSE_INDEX.md are created.
  </action>
  <verify>
    <automated>echo "=== Section INDEX.md count ===" && find output/transcripts -name "INDEX.md" -maxdepth 2 | wc -l && echo "=== COURSE_INDEX.md exists ===" && test -f output/COURSE_INDEX.md && echo "YES" || echo "NO" && echo "=== No absolute paths ===" && grep -r "/Users/" output/COURSE_INDEX.md output/transcripts/*/INDEX.md 2>/dev/null | wc -l && echo "=== Section 12 in course index ===" && grep -c "putting-everything-together" output/COURSE_INDEX.md && echo "=== All sections in course index ===" && grep -c "^## \[" output/COURSE_INDEX.md</automated>
  </verify>
  <done>13 INDEX.md files exist (one per section directory). 1 COURSE_INDEX.md exists at output/ root. 0 absolute paths in any index file. Section 12 present in COURSE_INDEX.md. All 13 sections listed in COURSE_INDEX.md. All links use relative paths.</done>
</task>

<task type="auto">
  <name>Task 2: Validate index links resolve correctly</name>
  <files></files>
  <action>
Validate that every markdown link in every index file resolves to a real file on disk.

**Validation approach:**
1. Read `output/COURSE_INDEX.md`
2. Extract all markdown links: `[text](path)`
3. For each link, resolve the path relative to `output/` and check `existsSync()`
4. Report any broken links

5. For each section `INDEX.md`:
6. Extract all markdown links
7. Resolve relative to the section directory
8. Check `existsSync()`
9. Report any broken links

**Expected results:**
- 0 broken links in COURSE_INDEX.md
- 0 broken links in any section INDEX.md
- All 95 video files are linked from their section INDEX.md
- All SECTION_SUMMARY.md files are linked
- All CHEAT_SHEET.md files are linked (where they exist)
- MASTER_SUMMARY.md is linked from COURSE_INDEX.md

**Validation script (inline — no separate file needed):**
Run a quick Node.js one-liner or write a small validation check. The key assertions:
- `find output/transcripts -name "INDEX.md" | wc -l` = 13
- `test -f output/COURSE_INDEX.md` passes
- No links in any index file point to non-existent files
- No absolute paths (`/Users/`, `C:\`, etc.)

Log results as:
```
INDEX VALIDATION
================
COURSE_INDEX.md: X links, 0 broken
01-introduction-to-the-course/INDEX.md: X links, 0 broken
...
Total: Y links, 0 broken
```
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const path = require('path');
const outDir = 'output';
let broken = 0, total = 0;

// Check COURSE_INDEX.md
const ci = fs.readFileSync(path.join(outDir, 'COURSE_INDEX.md'), 'utf8');
const ciLinks = [...ci.matchAll(/\]\(([^)]+)\)/g)].map(m => m[1]);
for (const link of ciLinks) {
  total++;
  if (!fs.existsSync(path.join(outDir, link))) { broken++; console.log('BROKEN: ' + link); }
}

// Check section INDEX.md files
const tDir = path.join(outDir, 'transcripts');
for (const sec of fs.readdirSync(tDir)) {
  const idxPath = path.join(tDir, sec, 'INDEX.md');
  if (!fs.existsSync(idxPath)) continue;
  const idx = fs.readFileSync(idxPath, 'utf8');
  const links = [...idx.matchAll(/\]\(([^)]+)\)/g)].map(m => m[1]);
  for (const link of links) {
    total++;
    if (!fs.existsSync(path.join(tDir, sec, link))) { broken++; console.log('BROKEN: ' + sec + '/' + link); }
  }
}
console.log('Total links: ' + total + ', Broken: ' + broken);
process.exit(broken > 0 ? 1 : 0);
"</automated>
  </verify>
  <done>All markdown links in COURSE_INDEX.md and all 13 section INDEX.md files resolve to existing files. 0 broken links. All 95 video files, all SECTION_SUMMARY.md files, all CHEAT_SHEET.md files, and MASTER_SUMMARY.md are reachable via the index hierarchy.</done>
</task>

</tasks>

<verification>
1. `find output/transcripts -name "INDEX.md" -maxdepth 2 | wc -l` returns 13
2. `test -f output/COURSE_INDEX.md` passes
3. `grep -r "/Users/" output/COURSE_INDEX.md output/transcripts/*/INDEX.md 2>/dev/null | wc -l` returns 0 (no absolute paths)
4. `grep -c "^## \[" output/COURSE_INDEX.md` returns 13 (all sections listed)
5. Link validation script reports 0 broken links
6. `grep "putting-everything-together" output/COURSE_INDEX.md` finds section 12 (not omitted)
</verification>

<success_criteria>
- 13 section INDEX.md files exist, each linking to all video files and reference files in that section
- 1 COURSE_INDEX.md exists at output/ root linking to all sections, videos, and reference files
- All links are relative paths (0 absolute paths)
- 0 broken links across all index files
- Section 12 (0 video files) is present in indexes with graceful handling
- Section 02 YouTube stub files appear in their section index
- Re-running generate-indexes.mjs produces identical output (idempotent)
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-and-navigation/04-02-SUMMARY.md`
</output>
